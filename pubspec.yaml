name: duoshare
description: DuoShare LAN Transfer App
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  nsd: ^5.0.1
  web_socket_channel: ^3.0.3
  permission_handler: ^12.0.1
  path_provider: ^2.1.5
  file_picker: ^11.0.2
  provider: ^6.1.5+1
  sqflite: ^2.4.2
  desktop_drop: ^0.4.4
  flutter_local_notifications: ^21.0.0
  open_file: ^3.5.10
  path: ^1.9.1
  device_info_plus: ^12.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
